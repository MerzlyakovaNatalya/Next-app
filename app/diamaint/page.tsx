// import dynamic from 'next/dynamic';

// const RemoteSlider = dynamic(() => import('project/react' as any), {
//   ssr: false,
//   suspense: true,
//   loading: () => <p>Loading Slider...</p>,
// })

const Diamaint = () => {
  return <>
  <h1>Diamaint</h1>
  {/* <RemoteSlider/> */}
  </>
};

export default Diamaint;
